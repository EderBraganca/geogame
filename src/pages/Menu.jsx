const Menu = () => {
     /*
    const getRandomLocation = async () => {
        const location = await randomStreetView.getRandomLocation(1);
        console.log(location);
        return location;
    };

    const startGame = () => {
        const location = getRandomLocation();
        window.location.href = `/game?location=${location}`;
    };
    */
    return (
        <div>
            <h1>Menu</h1>
        </div>
    );
};

export default Menu;
//<button onClick={startGame}>Start Game</button>